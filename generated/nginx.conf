# Generated by ananke from the 'ananke-demo' topology.
# Source of truth: src/infra.zig
# Do not edit; this file is rewritten by `zig build emit` and every
# byte of it was verified before the binary was allowed to exist.

upstream gateway_http {
    server gateway:8080;
}

upstream api_http {
    server api:8081;
}

server {
    listen 443 ssl;
    server_name _;
    ssl_certificate     /etc/ssl/certs/edge.crt;
    ssl_certificate_key /etc/ssl/private/edge.key;
    ssl_protocols       TLSv1.2 TLSv1.3;

    # everything the public sees
    location /gateway/ {
        proxy_pass http://gateway_http/;
        proxy_set_header Host              $host;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        return 404;
    }
}

